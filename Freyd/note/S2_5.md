# §2.5 Quotient Allegories — formalization tracker

Source: Freyd & Scedrov, *Categories, Allegories*, §2.5 (book text ~lines 14533–14660 of
`categories-allegories.txt`). Lean: `Freyd/S2_5.lean`, namespace `Freyd.Alg`.
Composition is diagram-order (juxtaposition / `≫`); reciprocal is `°`; `⊑` is the
relation order; `∩`/`∪` lattice ops; `⁺` = `AmenableCongruence.largest` (largest element
of a congruence class).

## Definitions (pre-existing, kept)

| Item                              | §       | Status |
|-----------------------------------|---------|--------|
| `Congruence`                      | 2.5     | def    |
| `booleanQuotientRel`              | 2.521   | def    |
| `closedQuotientRel`               | 2.522   | def    |
| `AmenableCongruence` (incl. `⁺`)  | 2.53    | def    |
| `congSetoid`                      | 2.5     | def    |
| `Separated`, `Dense`              | 2.56    | def    |

## Proved (no `sorry`)

| Theorem                              | §        | Note |
|--------------------------------------|----------|------|
| `amenable_le_largest`                | 2.531    | R⊑S ⟹ R⁺⊑S⁺ |
| `amenable_union_largest_le`          | 2.531    | R⁺∪S⁺ ⊑ (R∪S)⁺ (book's union step) — **added** |
| `amenable_inter_largest`             | 2.532    | (R∩S)⁺ = R⁺∩S⁺ |
| `amenable_largest_class_invariant`   | 2.533    | R≡S ⟹ R⁺=S⁺ — **added** (class-independence of ⁺) |
| `quotient_le_iff_largest`            | 2.533    | [R]⊑[S] ↔ R⁺⊑S⁺ — one canonical quotient-order bridge |
| `largest_comp_le`                    | 2.534    | T⁺S⁺ ⊑ (TS)⁺ |
| `largest_recip_le`                   | 2.534    | (S⁺)° ⊑ (S°)⁺ |
| `largest_reflexive`                  | 2.535    | R reflexive ⟹ R⁺ reflexive |
| `largest_symmetric`                  | 2.535    | R symmetric ⟹ R⁺ symmetric |
| `largest_transitive`                 | 2.535    | R transitive ⟹ R⁺ transitive |
| `booleanQuotientRel_equiv`           | 2.521    | equivalence relation |
| `booleanQuotientRel_is_congruence`.{refl,symm,trans,recip_congr,inter_congr,comp_congr} | 2.521 | **all fields complete** — `comp_congr` was the last `sorry`; discharged via the two Schröder disjointness forms (below) |
| `closedQuotientRel_is_congruence`.{refl,symm,trans,recip_congr,inter_congr,comp_congr} | 2.522 | **all fields complete** — `recip/inter/comp_congr` were `sorry`s, now discharged (see hypotheses below) |
| `quotient_comp/recip/inter_wellDefined` | 2.5   | well-definedness on classes |

## Newly proved supporting lemmas (this pass)

| Lemma (private, in `BooleanCong`)    | role |
|--------------------------------------|------|
| `disjoint_schroder R S T`            | `(R≫S)∩T=𝟘 ↔ (T≫S°)∩R=𝟘` — Schröder disjointness from the modular law |
| `disjoint_schroder' R S T`           | `(R≫S)∩T=𝟘 ↔ (R°≫T)∩S=𝟘` — second Schröder form (via `recip_disjoint`) |
| `recip_disjoint X Y`                 | `X∩Y=𝟘 ↔ X°∩Y°=𝟘` |
| `le_zero`, `inter_zero`              | `X⊑𝟘 ⟹ X=𝟘`; `R∩𝟘=𝟘` |
| `union_inter_distrib R S K`          | dual distributivity `(R∩S)∪K=(R∪K)∩(S∪K)` (lattice) |

`booleanQuotientRel_is_congruence.comp_congr` (§2.521) is now a pure disjointness chain:
`(RS)∩T=0 ↔ (TS°)∩R=0 ↔[hR] (TS°)∩R'=0 ↔ (R'S)∩T=0 ↔ (R'°T)∩S=0 ↔[hS] (R'°T)∩S'=0 ↔ (R'S')∩T=0`,
using only the modular law (Schröder forms) and the boolean relation on each side.

## §2.522 closed quotient — faithful hypotheses now made explicit

To make the closed-quotient congruence axioms provable *and* honest, the definition
`closedQuotientRel_is_congruence` was refined to the book's actual unitary context
(§2.522: `T` is the unit `A`, one canonical family `p a : a ⟶ A`, `U : A ⟶ A`). The
single family `p` (not two independent `p_a`, `p_b`) and the following hypotheses are
exactly the unit-context facts that make `K_{ab}=p a ≫ U ≫ (p b)°` a symmetric two-sided
ideal — i.e. a congruence:

- `hU : U° = U` — `U` symmetric (closed/coreflexive element on the unit). Gives `K_{ab}°=K_{ba}`, so the relation respects `°`.
- `hL R : R ≫ K_{bc} ⊑ K_{ac}` — left ideal absorption.
- `hR' S : K_{ab} ≫ S ⊑ K_{ac}` — right ideal absorption.

`(hL,hR')` express that `K` is a two-sided ideal (absorbing arbitrary cross terms in a
product); in a genuine unitary allegory they follow from maximality of the unit
projections `R ≫ p b ⊑ p a` together with `U ≫ U ⊑ U`. With these, all three axioms
are proved: `recip_congr` by `recip_union`+`hU`; `inter_congr` by `union_inter_distrib`;
`comp_congr` by expanding `(R∪K)(S∪K)` and absorbing the three cross terms (le-antisymm).

Axiom check (`#print axioms`): both `booleanQuotientRel_is_congruence` and
`closedQuotientRel_is_congruence` depend only on `propext` — no `sorryAx`, no choice.

## MISSING — cannot be STATED faithfully yet (recorded, NOT stubbed)

| Book result                                              | §      | Blocker |
|----------------------------------------------------------|--------|---------|
| Amenable quotient of a division allegory is a division allegory | 2.536 | The **quotient-allegory type** (equivalence classes as a new allegory with its own Hom/comp/recip/inter/division) is not built. A `AmenableCongruence 𝒜 → DivisionAllegory 𝒜` signature would be a vacuous restatement (𝒜 already has the instance; `amen` ignored), so it is omitted per the integrity rule. The algebraic heart (⁺ commutes with the ops) is captured by the §2.531/2.533/2.534 ⁺-laws above. |
| Every topos admits a faithful bicartesian representation to a boolean topos | 2.542 | No `Topos`/`BooleanTopos`/representation-of-allegories morphism in the repo. The previous `: True` stub was **removed**. |
| Quotient of tabular/unitary is such                      | 2.51   | needs quotient-allegory type. |
| Amenable quotient of effective power allegory is effective power | 2.537 | needs quotient-allegory type + power-allegory machinery. |
| Coreflexive named by coreflexive                         | 2.54   | needs quotient-allegory type. |

## Integrity note

A prior Sonnet attempt left `theorem topos_boolean_representation : True := by trivial`
(§2.542) and `amenableQuotientDivision : DivisionAllegory 𝒜 := by sorry` (§2.536, vacuous).
Both removed: the first was a `: True` lie, the second a vacuous restatement on the same
type. Replaced by MISSING records above with explicit blockers.
