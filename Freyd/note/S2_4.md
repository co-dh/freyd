# §2.4 Power Allegories — Tracker

| Section | Description                                    | Kind  | Status  | Lean name / notes                                                                                 |
|---------|------------------------------------------------|-------|---------|---------------------------------------------------------------------------------------------------|
| §2.41   | PowerAllegory class (eps, straight, thick)     | class | DONE    | `PowerAllegory`; `eps_thick` now FAITHFUL §2.413 form (see "Resolved blocker")                   |
| §2.412  | A(R) = R/ₛ∋ is a map, A(R)∋ = R               | thm   | DONE    | `A_is_map` (simple+entire ✓); `A_eps_eq` (both ⊑ and ⊒ ✓)                                       |
| §2.412  | A(R) is unique map with A(R)∋ = R              | thm   | DONE    | `A_unique` — proved using Entire F + straightness of ∋                                           |
| §2.412  | Simple F ⊑ A(F∋)                               | thm   | DONE    | `simple_le_A_eps`                                                                                 |
| §2.414  | Topos ↔ unitary tabular power allegory         | note  | MISSING | Recorded MISSING (no vacuous stub); needs Rel(C)/Map(A) bridge to S1_9 topos                     |
| §2.415  | Power object [a], singleton map A(1_a)         | def   | DONE    | `singletonMap`                                                                                    |
| §2.415  | Singleton map is monic                         | thm   | DONE    | `singletonMap_monic`                                                                              |
| §2.415  | A(f) = f ≫ A(1) for maps f                    | thm   | DONE    | `A_of_map` — via `A_unique` + `map_comp` + `A_eps_eq`                                            |
| §2.13   | Composition of maps is a map (helper)          | thm   | DONE    | `map_comp` (general §2.13; lives in S2_4 due to edit-scope; candidate to hoist into S2_1)        |
| §2.421  | R/ₛS = A(R) ≫ (A S)° (splitting formula)      | thm   | DONE    | `symm_div_eq_A_comp` (both bounds ✓; lower bound uses A(R) entire + A_eps_eq)                    |
| §2.422  | Every eq. rel. is ff° in a power allegory      | note  | MISSING | Not formalized; needs A_eps_eq + splitting coreflexives                                           |
| §2.43   | PrePowerAllegory class (thick morphism)        | class | DONE    | `PrePowerAllegory`; `Thick` def CORRECTED to book-faithful symmetric form `∀R(R□=T□), Entire(R/ₛT)` |
| §2.431  | Thickness characterization (existential)       | thm   | DONE    | `thick_iff_existential` — FULLY PROVEN (both directions), `propext` only; statement repaired with `R□=T□` hyp |
| §2.354  | Straight factorization in eff. div. allegory   | thm   | DONE    | `straight_factorization` — every `T:x→a` = `h≫S`, `h` map, `h°h=1`, `S=h°≫T` straight. Splits `E=T/ₛT`. `propext` only. New class `EffectiveDivisionAllegory` (= Division+Effective, one shared Allegory, no diamond) |
| §2.432  | codomain box preserved by split (`T°T=S°S`)    | thm   | DONE    | `codBox_eq_of_split` — `h°h=1, T=h≫S ⟹ codBox S = codBox T`. Axiom-free                          |
| §2.432  | thickness descent (`S=h°≫T` stays thick)       | thm   | DONE    | `straight_descent_thick` — `Thick T, T=h≫S, h map, h°h=1 ⟹ Thick S` (box-guarded §2.43 form). Witness `R̃=(R/ₛT)≫h`. `propext` only |
| §2.432  | each obj has straight-thick target             | thm   | DONE    | `exists_straight_thick_target` — assembles §2.354+descent. `propext` only                        |
| §2.416  | straight map is monic (`h≫h° ⊑ 1`)             | thm   | DONE    | `straight_map_monic` — `Map h, Straight h ⟹ h≫h° ⊑ 1` via `hh° ⊑ h/ₛh ⊑ 1`. **Axiom-free.** The monic half of §2.416 maximality |
| §2.416  | split-factor map is monic                      | thm   | DONE    | `straight_factor_map_monic` — `S=h≫S', Straight S, Map h ⟹ h≫h° ⊑ 1` (h straight by §2.355, then `straight_map_monic`). `propext` only |
| §2.432  | Effective pre-power allegory is power          | thm   | SORRY   | `effective_pre_power_is_power` — sorry CONFINED to one field `eps_thick`; `powerObj`/`eps`/`eps_straight` built honestly from straight-thick target. Class `EffectivePrePowerAllegory`. Remaining gap = box-guarded `Thick S` → unconditional §2.413 `∀R∃f, fS=R`. **Assessed (this session): genuinely needs a PROGENITOR (§1.966)** — both the copower object `coprod (powerObj b) p` (§2.416 step 1, for the cotuple `(R;∋)`) AND the progenitor's separating property for the epic half `1 ⊑ h°h` of maximality (step 3). The monic half of maximality IS proven (`straight_map_monic`/`straight_factor_map_monic`); only the copower + separator are absent, and adding them would weaken §2.43's hypotheses |
| §2.441  | PrePositiveAllegory, WellJoinedAllegory        | class | DONE    | `PrePositiveAllegory`, `WellJoinedAllegory`                                                       |
| §2.441  | Pre-positive → well-joined                     | thm   | DONE    | `pre_positive_to_well_joined`                                                                     |
| §2.441  | Equivalences: pre-positive ↔ well-joined ↔ ... | thm   | MISSING | Not formalized; multi-way equivalence (requires §2.421 full proof)                               |
| §2.442  | bigInter, bigUnion, subset                 | def   | DONE    | `bigInter`, `bigUnion`, `subset`                                                              |
| §2.442  | MetonymyLaw (⊓ ⊑ ⊔)                           | def   | DONE    | `MetonymyLaw`                                                                                     |
| §2.442  | `∋ ≫ A(1) ⊑ 2 = ∋/∋` (order step)             | thm   | DONE    | `eps_singleton_le_subset` — `propext` only. Via `le_div_iff` + `A_eps_eq` (A(1)∋=1)          |
| §2.16(10) | Composition of simples is simple             | thm   | DONE    | `simple_comp` — `(fg)°(fg)=g°(f°f)g ⊑ g°g ⊑ 1`. `propext` only                                  |
| §2.16(10) | Semi-simple ≫ simple is semi-simple          | thm   | DONE    | `semiSimple_comp_simple` — `R=F°G, H simple ⟹ RH=F°(GH)` (`simple_comp`). `propext` only        |
| §2.16(10) | Contained-in-semisimple is semi-simple       | thm   | DONE    | `semiSimple_of_le` — `R ⊑ F°G ⟹ R = F°((1∩FRG°)G)`, the coref `G'` simple via `simple_coref_comp`. Modular law both bounds. `propext` only |
| §2.442  | A(S) monic for straight S (`A(S)A°(S) ⊑ 1`)    | thm   | DONE    | `A_monic_of_straight` — `A(S)A°(S) ⊑ S/ₛS ⊑ 1` via `le_symmDiv_iff` + `Straight S`. `propext` only |
| §2.442  | A°(S) simple for straight S                     | thm   | DONE    | `A_recip_simple` — `Simple (A S)° ⟺ A(S)A°(S) ⊑ 1` = `A_monic_of_straight`. `propext` only |
| §2.442  | straight S semi-simple if ∋ semi-simple         | thm   | DONE    | `straight_semiSimple_of_eps_semiSimple` — `S = A(S)∋ = (F≫A(S)°)°≫G` (`A_eps_eq`, `A_recip_simple`, `simple_comp`). The book's "S = A(S)∋ is semi-simple". `propext` only |
| §2.442  | forward assembly: R = S≫F ⟹ R semi-simple       | thm   | DONE    | `semiSimple_of_straight_simple_factor` (private) — metonymy⟹∋ ss⟹S ss; `semiSimple_comp_simple`. Single `PowerAllegory` instance (diamond-free). `propext`+sorry via GAP 1 |
| §2.442  | Pre-pos. power allegory semi-simple ↔ metonymy | thm   | SORRY   | `pre_positive_semi_simple_iff_metonymic` — forward reduced to exactly GAP 1 (`eps_semiSimple_of_metonymy`) + GAP 2 (§2.441 (1)⟹(4)), connecting algebra all PROVEN (`A_monic_of_straight`, `A_recip_simple`, `straight_semiSimple_of_eps_semiSimple`, `semiSimple_of_straight_simple_factor`). 3 sorries total (incl. converse) |

## Resolved blocker: eps_thick axiom (was the §2.4 root blocker)

The previous axiom `eps_thick (b) : 1 ⊑ ∋/∋` was **vacuously true** in any division allegory
(`one_le_div_self`), carrying no information. It has been replaced by Freyd's own §2.413 form,
which IS the thickness condition:

```
eps_thick {b c} (R : c ⟶ b) : ∃ (f : c ⟶ powerObj b), Map f ∧ f ≫ eps b = R
```

"for every R targeted at b there exists a map f with f∋ = R." §2.413 proves this forces
`A(R) = R/ₛ∋` entire (and §2.41's third containment `1 ⊑ A(R)A°(R)` is exactly this entireness).
This is faithful to the book (it is the condition Freyd uses to *infer* thickness) and is
constructive + non-vacuous. Changing it was in-scope (the class lives in S2_4) and broke no
downstream code (`eps_thick` had no other users; no `PowerAllegory` instances exist yet).

With this fix the whole §2.41–§2.421 chain goes through constructively (axioms: `propext` only):
- `A_is_map` entire branch: thickness witness `f ⊑ A R` (via `thick_witness_le_A`), so
  `1 ⊑ ff° ⊑ (A R)(A R)°` ⟹ `dom(A R) = 1`.
- `A_eps_eq` lower bound: `R = f∋ ⊑ (A R)∋` since `f ⊑ A R`.
- `A_of_map`: `f≫A(1)` is a map (`map_comp`) with `(f≫A(1))∋ = f` (`A_eps_eq`), so `=A(f)` by `A_unique`.
- `symm_div_eq_A_comp` lower bound: `(R/ₛS)°≫A R ⊑ A S` (symmDiv char), reciprocate, then use
  `A R` entire to insert `A R ≫ (A R)°` in front of `R/ₛS`.

## Resolved: §2.431 `thick_iff_existential` — STATEMENT REPAIRED + FULLY PROVEN

The earlier formulation was LOGICALLY FALSE. Two independent defects, both now fixed:

1. **`Thick` used the wrong division.** The book's thickness (§2.412/§2.43) is the entireness
   of `Δ(R) = R/ₛ∋`, the **symmetric** division — generalised here to `∀R, Entire (R/ₛT)`.
   The old def `∀R, Entire (R/T)` used the **plain** asymmetric right division; this predicate
   is strictly stronger and breaks the forward direction.
2. **The book's `R□ = T□` side-condition was dropped.** §2.431 quantifies only over R with the
   same codomain box (`R□ = 1_b ∩ R°R`); that guard is the domain on which Freyd's partial
   division `R/T` is defined. Restoring it (on BOTH `Thick`'s quantifier and the iff) is
   what makes the biconditional hold.

**Counterexample is now excluded correctly.** T = {(0,0)}:{0,1}→{0}, R = {(0,0),(1,0)} satisfy
`R□ = T□` and T is thick, so the corrected statement still applies — and it is SATISFIED: the
honest witness is `R̃ = R/ₛT = {(0,0),(1,0)}`, which is entire with `R̃T ⊑ R`, `R̃°R ⊑ T`. The old
statement was false only because it implicitly forced the witness to be the plain `R/T`, whose
`(R/T)°R = R ⊄ T`.

**Verification.** Both directions checked exhaustively in Rel (all relations, source/target sizes
up to 3×3): 0 forward violations, 0 reverse violations under the corrected statement. Dropping
the box guard re-introduces reverse violations; using plain `R/T` re-introduces forward violations.

**Proof.** Forward: witness `R̃ = R/ₛT`, entire directly from `Thick T` (which consumes the box
hypothesis); the two containments are the `le_symmDiv_iff` law on `R/ₛT ⊑ R/ₛT`. Reverse: `R̃ ⊑ R/ₛT`
and `R̃` entire force `R/ₛT` entire, via the constructive helper `entire_iff_one_le`. Axioms: `propext`
only (no choice, no `sorryAx`).

## Sorry Summary (remaining — all flagged deep, none are the §2.4 named targets)

| Name                                  | Why sorry — sharpened blocker                                                                |
|---------------------------------------|----------------------------------------------------------------------------------------------|
| `effective_pre_power_is_power` (§2.432) | Sorry CONFINED to the single field `eps_thick`. §2.354 (`straight_factorization`) and the §2.432 descent (`straight_descent_thick`, `codBox_eq_of_split`) ARE proven, so `powerObj`/`eps`/`eps_straight` are built honestly. The one gap: our `Thick S` (§2.43, box-guarded `∀R(R□=S□), Entire(R/ₛS)`) yields the witness only when `codBox R = codBox S`, but `PowerAllegory.eps_thick` is the *unconditional* §2.413 form `∀R∃f map, fS=R`. **Precise missing primitive (this session's assessment):** a PROGENITOR `y` (§1.966), required for BOTH halves of Freyd's §2.416 inference: **(step 1)** the binary copower / coproduct OBJECT `coprod (powerObj b) p` with `PositiveAllegory.has_coproduct`, needed to form the cotuple `(R;∋):(powerObj b ⊕ p)→b` and straighten it — absent because `EffectivePrePowerAllegory = DivisionAllegory + EffectiveAllegory` gives `∪`/`𝟘` on hom-sets but no coproduct objects, and effectiveness only splits idempotents over a *single* object (`split_symmetric_idempotent`); **(step 3, epic half)** the progenitor's *separating* property, needed for `1 ⊑ h°h` (`box-guarded Thick S` does not force `h` epic). The **monic half** `h≫h° ⊑ 1` of maximality IS now proven this session (`straight_map_monic`, `straight_factor_map_monic`, axiom-free / `propext`). §2.43 pre-power allegories are not assumed positive, so supplying the progenitor/copower as instance fields would weaken the theorem below the book's hypotheses — hence the faithful single sorry. |
| `pre_positive_semi_simple_iff_metonymic` (§2.442) | FAITHFUL SORRY, forward direction now REDUCED to exactly two named gaps with all connecting algebra PROVEN this session. **Proven forward chain:** metonymy ⟹ ∋ semi-simple (GAP 1) ⟹ every straight `S` semi-simple (`straight_semiSimple_of_eps_semiSimple`: `S = A(S)∋ = (F≫A(S)°)°≫G`, using `A_eps_eq`, `A_recip_simple` = `A_monic_of_straight`, `simple_comp`) ⟹ every `R = S≫F` semi-simple (`semiSimple_of_straight_simple_factor` = `semiSimple_comp_simple`). **GAP 1** (`eps_semiSimple_of_metonymy`, private sorry): the *metonymy ⟹ `2 = ∋/∋` semi-simple* link; honest reductions `∋A(1) ⊑ 2` (`eps_singleton_le_subset`) and `2≫∋ ⊑ ∋` (`div_comp_eq_le`) ARE in place, but turning `MetonymyLaw` into `SemiSimple (∋/∋)` needs the bigUnion/bigInter `A`-calculus (`A(f∪g)≫⊔ = f∋∪g∋`, `⊓(f∪g)∩∋ = f∋∩g∋`), unbuilt. **GAP 2** (§2.441 (1)⟹(4)): every `R:a→b` = `S≫F` (`S` straight, `F` simple) via the pre-positive `R̃` — `pre_positive` covering maps available, `R̃`/`S`/`F` assembly unbuilt. **STRUCTURAL note:** GAP 2 cannot even be *stated* inline mixing `Straight` (the `PowerAllegory` DivisionAllegory) with `pre_positive` (the `PrePositiveAllegory` DistributiveAllegory) — the two parents give *distinct* `Allegory 𝒜` instances, a diamond that breaks `Straight S` unification for a freshly-bound `S:a⟶c` (verified: `?m Cat.Hom` mismatch). A faithful §2.441 dev first needs a single combined `PrePositivePowerAllegory` class (outside this file's edit scope). **CONVERSE** (every morphism ss ⟹ metonymy): single sorry, same bigUnion/bigInter calculus as GAP 1. Total 3 sorries (GAP 1 + converse + forward GAP 2). |

## MISSING (not formalized)

- §2.422: In a power allegory, every equivalence relation E = ff° for some map f.
- §2.441: Full 4-way equivalence (pre-positive ↔ well-joined ↔ connected+SF-factorization ↔ ...).
- §2.423: Connected power allegory with coreflexives split has a unit.
- §2.424: Connected semi-simple power allegory → topos.
- §2.433–§2.435: Systemic completion is pre-power; Cantor's theorem.
- §2.436: One-object pre-power allegory is inconsistent.
- §2.437–§2.438: Halting problem, Gödel incompleteness corollaries.
- §2.443: Semi-simple iff ⊓ ⊑ ⊔ iff union of semi-simple morphisms.
- §2.444: Law of metonymy is independent.
- §2.445–§2.446: Derived 0 and ∪ from power allegory structure.
