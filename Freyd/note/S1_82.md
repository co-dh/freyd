# §1.82–§1.83 Formalization Tracker

## Declarations (Freyd/S1_82.lean)

| Declaration | Status | Notes |
|---|---|---|
| `DiagCone` | done | cone of diagram D with apex B; nat condition |
| `DiagCocone` | done | cocone with nadir B |
| `HasLimit` | done | universal cone with unique lift |
| `HasColimit` | done | universal cocone |
| `Complete` | done | class: every small diagram has a limit |
| `Cocomplete` | done | class: every small diagram has a colimit |
| `HasProducts` | done | class: all small indexed products |
| `discreteCat` | done | private: discrete category on I |
| `discreteFunctor` | done | private: every F : I → ℬ is functor on discrete cat |
| `complete_hasProducts` | done | Complete → HasProducts (via discrete limits) |
| `WPP / WPPHom / wppCat` | done | private: walking parallel pair category |
| `complete_hasEqualizers` | done | easy direction; WPP lifted to `Type v` (`WPPv`); limit of parallel pair |
| `eq_prod_complete` | done | hard direction: HasEqualizers + HasProducts → Complete |
| `complete_iff_eq_prod` | done | both directions proved (axioms: `propext`, `Quot.sound` only) |
| `IsContinuous` | done | F preserves all small limits |
| `IsCocontinuous` | done | F preserves all small colimits |
| `HasWeakLimit` | done | cone with existence but not uniqueness |
| `WeaklyComplete` | done | class |
| `complete_imp_weaklyComplete` | done | instance: Complete → WeaklyComplete |
| `preserves_weaklim_iff_preserves_lim` | done | full §1.829 proof; monic family via `MFShape` J-poset (axiom-free) |
| `HasPreLimit` | done | cofinal set of cones |
| `PreComplete` | done | class |
| `complete_imp_preComplete` | done | instance: singleton pre-limit from limit |
| `PreAdjointObj` | done | cofinal set of maps A → G(Bᵢ) |
| `PreAdjointFunctor` | done | class |
| `preAdjointFunctor_of_adjunction` | done | left adjoint ⟹ pre-adjoint (unit = singleton pre-adjoint family); axiom-free |
| `isContinuous_of_adjunction` | done | left adjoint ⟹ G continuous (right adjoint preserves limits); axiom-free |
| `adjunction_of_representability` | **done (axiom-free)** | §1.817 bridge: per-`A` representability ⟹ left adjoint. Pure universal-property bookkeeping; the sole copy (the old `S1_8.adj_of_repr` duplicate was removed) |
| `InitialElement` / `.represents` | **done (new)** | initial object of `El(A,G-)` (element `θ` + existence + uniqueness of factoring maps) ⟹ `RepresentedBy G A R` |
| `WideEqualizer` / `wideEqualizer` | **done (new, axiom-free)** | joint equalizer of a small family `K → (P⟶P)` against `id`, built as `eq(tupling e, tupling const-id)` into `∏ₖ P` (equalizers + products) |
| `gaft_representability` | **done (axiom-clean)** | §1.834–35 representing-object engine: solution set ⟹ product `P` (continuity ⟹ element `η`) ⟹ wide equalizer `R` of `A↓G`-endos (continuity ⟹ element `θ` and uniqueness); `r` monic |
| `general_adjoint_functor_theorem` | **FULLY PROVED** | both directions; `lean_verify` axioms = `propext`, `Classical.choice` (no `sorryAx`) |
| `limit_cone_unique` | done | any two limit cones are iso |
| `IsUniformlyContinuous` | done | G sends cones to cofinal families |
| `uniformly_continuous_preserves_prelimits` | done | via cofinal argument |
| `isUniformlyContinuous_of_adjunction` | done | left adjoint ⟹ G uniformly continuous; no completeness needed; axiom-free |
| `IdempotentSplits` / `IdempotentsSplit` | **done** | §1.281 idempotent-splitting property, restated locally (avoids the heavy `S1_39` import chain) |
| `wforkDiag*` / `WForkv` | **done (new)** | wide-fork diagram shape: 2 objects `src,tgt`, arrows `src→tgt` indexed by `Option K` (`none ↦ id_P`, `some k ↦ e k`). A *cone* over it is a map `q` with `q ≫ e k = q` for every `k` — the "weak wide equalizer" used instead of a genuine one under `PreComplete` |
| `mgaft_representability` | **FULLY PROVED** | MGAFT step: uniformly continuous + pre-adjoint + **pre-complete** + idempotents-split ⟹ representable (§1.835). Does NOT reuse the GAFT engine (which needs full `Complete`). Construction: discrete pre-limit + UC ⟹ weakly-initial `(P,η)`; wide-fork pre-limit over the η-fixing endos `M` + UC ⟹ a map `q` with `q ≫ e = q ∀ e∈M` and element `η_Q`; weak-initial factoring of `η_Q` gives `e₀ := s≫q` with `e₀∈M` and `e₀ ≫ e = e₀ ∀ e∈M`, hence **`e₀` idempotent**; `IdempotentsSplit` splits `e₀ = ρ≫σ`, `σ≫ρ=id_R`; then `(R, θ_R := η≫Gρ)` is an **initial element**, existence via weak initiality, uniqueness via `σ ≫ e = σ ∀ e∈M` + a fresh wide-fork weak-equalizer of each candidate pair. `lean_verify` axioms = `propext`, `Classical.choice` (no `sorryAx`) |
| `more_general_adjoint_functor_theorem` | **FULLY PROVED** | both directions; the essential `IdempotentsSplit ℬ` hypothesis (without it the theorem is FALSE, Freyd §1.836) is load-bearing — it is exactly what cuts the weakly-initial `(P,η)` to an initial element. `lean_verify` axioms = `propext`, `Classical.choice` (no `sorryAx`) |
| `HasPreColimit` | **done (new)** | faithful colimit-dual of `HasPreLimit`: cofinal family of COCONES |
| `PreCocomplete` | **done; MODELING FIXED** | now uses `HasPreColimit` (cofinal cocones), not `HasPreLimit` (cones = lower bounds, wrong-variance for colimits) |
| `cocomplete_imp_preCocomplete` | **done (axiom-free)** | easy half of §1.837: singleton pre-colimit from the colimit (dual of `complete_imp_preComplete`) |
| `cocomplete_of_complete_precocomplete` | **done (axiom-clean)** | hard half, built CONSTRUCTIVELY as the colimit-dual of the GAFT engine — NO functor category needed: `P := ∏ⱼ Nⱼ` over pre-colimit nadirs ⟹ weakly-initial cocone `(P,κ)` ⟹ wide equalizer `R` of cocone-endos ⟹ colimit `(R, ιR)`; uniqueness via `r` monic + split-epi |
| `complete_cocomplete_iff_precocomplete` | **FULLY PROVED** | both directions; `lean_verify` axioms = `propext`, `Classical.choice` |
| `SubobjectIso` | done | iso of subobjects |
| `WellPowered` | done | class: subobjects form a set |
| `IsCoGeneratingSet` | done | {Hom(-,Cᵢ)} collectively faithful |
| `cogenerating_embeds_in_product` | **done (new, axiom-clean)** | §1.83(10) embedding: with all products, a cogenerating set makes the evaluation map `B ⟶ ∏_{(i,h):B→Cᵢ} Cᵢ` MONIC (collective faithfulness read off projections) |
| `Cospan` / `gPullbackFactor` | **done (new)** | walking-cospan diagram + `G` continuous ⟹ `G` preserves the pullback of a mono: builds the pullback as a cospan limit, proves the `lft`-leg monic, and uses continuity to factor a commuting pair `(η,f)` through it (the §1.838 factoring step) |
| `saft_preadjoint` | **done (new, axiom-clean)** | §1.838 SOLUTION SET: for each `A`, family indexed by `Σ (k:WPidx PA), (A⟶G(reprPA k).dom)` over well-powered subobjects of `PA := ∏_{j:Σi,(A→GCᵢ)} Cⱼ.₁`; cofinality of `f:A→GB` via embed-`B` + comparison `w` + the square `η≫Gw=f≫GeB` (joint-monicity of `{G proj}` from continuity) + pull back `eB` along `w` + transport along the well-powered representative iso |
| `saft_representability` | **FULLY PROVED** | §1.838 SAFT step: `gaft_representability hcont (saft_preadjoint C hcogen hcont)` — solution set fed to the proven GAFT engine. `lean_verify` axioms = `propext, Classical.choice, Quot.sound` (no `sorryAx`) |
| `special_adjoint_functor_theorem` | **FULLY PROVED** | complete + well-powered + cogenerating + continuous ⟹ left adjoint; reduces through the proven §1.817 bridge to `saft_representability`; axioms = `propext, Classical.choice, Quot.sound` |

## Discharged this session

- **MGAFT (§1.831) FULLY PROVED** — `mgaft_representability` and hence
  `more_general_adjoint_functor_theorem` are now sorry-free (both directions). The hard `(⇐)`
  is the pre-limit + idempotent-splitting weak-initial-object cutoff (see the table row above);
  `IdempotentsSplit` is load-bearing. **S1_82 now has zero sorries.**
- **`(⇒)` of both adjoint-functor-theorem iffs** (GAFT §1.83, MGAFT §1.831): peeled off the
  EASY half of each iff as three standalone, axiom-free helpers and wired the `(⇒)` branch
  of each iff through them.
  - **`preAdjointFunctor_of_adjunction`**: a left adjoint is a pre-adjoint functor. The unit
    `η_A : A → G(F A)` is itself a *singleton* (`I := PUnit`) pre-adjoint family: any
    `f : A → G B` factors as `η_A ≫ G(ψ f) = φ(ψ f) = f` (via `φ_eq` + `φψ`, §1.817).
  - **`isContinuous_of_adjunction`**: a right adjoint preserves all small limits. Transpose
    the W-cone `{W → G(D i)}` to `{F W → D i}` via `ψ` (cone-naturality from `ψ_nat_right`),
    lift through the limit to `w : F W → lim.apex`, transpose back `u := φ w`. Uniqueness:
    any `u'` gives `ψ u'` mediating ⟹ `ψ u' = w` by `lim.uniq` ⟹ `u' = φ(ψ u') = φ w`.
    `lean_verify`: axiom-free.
  - **`isUniformlyContinuous_of_adjunction`**: same transpose, but no completeness needed —
    take `B := F A`, `cone_legs i := ψ(legs i)`, factoring map `η_A`. Axiom-free.

- **`complete_hasEqualizers`** (§1.825 ⇒ equalizer side): built `WPPv := ULift WPP : Type v`
  with `wppCatV : Cat.{v}` (ULifted homs), the parallel-pair diagram functor `wppDiagObj/Map`,
  and read off the equalizer from `Complete.hasLimit`. The `src`-leg is `eqMap`, the lift comes
  from the limit lift on the cone `{botL ↦ c.map, …}`, and uniq from `lim.uniq`. This closes the
  `(⇒)` of `complete_iff_eq_prod`, so that whole iff is now proved.

- **`preserves_weaklim_iff_preserves_lim`** (§1.829, the uniqueness gap): faithful formalization
  of Freyd's actual argument "preservation of weak-limits ⟹ preservation of monic families".
  Introduced the book's J-poset as `MFShape 𝒟` (discrete `𝒟` + two bottom points `botL/botR`,
  each below every `pt i`), with `mfDiagObj`/`mfDiagMap` sending `pt i ↦ Dᵢ`, `botL/botR ↦ L`,
  `arrL i / arrR i ↦ πᵢ`. The canonical lower bound is a weak-limit **iff** `{πᵢ}` is collectively
  monic (`limMonic`, proved from `lim.uniq`). Feeding this through the weak-limit-preservation
  hypothesis yields a lift `w` with `w ≫ T(id_L) = u` and `w ≫ T(id_L) = u'`, hence `u = u'`.
  Verified axiom-free (`lean_verify` returns no axioms).

## Discharged this session (2026-06-16)

- **`adjunction_of_representability`** (§1.817 ←, axiom-free): the representability→adjunction
  bridge. Given a per-object representation `r A : RepresentedBy G A (F A)`, defines `F` on maps
  by `Fmap f := (r A).φ (f ≫ η A')` with unit `η A := (r A).ψ (id (F A))`, proves functoriality
  and both adjunction-naturality laws. Key lemma `ηbridge : η A ≫ G ((r A).φ g) = g`. The A-side
  naturality (`φ_nat_left`) — which `RepresentedBy` does NOT carry directly — is derived by
  applying the bijection `(r A').φ` to both sides and reducing via `ηbridge`. (The `S1_8.adj_of_
  repr` copy is still `sorry`; this inline version is complete.)
- **All four target theorems are genuine proofs**, each reducing through the proven bridge to a
  single sharp, well-typed "build the representing object" lemma; **all four are now fully
  discharged** (see the next section):
  - `general_adjoint_functor_theorem` (⇐) → `gaft_representability` — **PROVED**
  - `complete_cocomplete_iff_precocomplete` (⇐) → `cocomplete_of_complete_precocomplete` — **PROVED**
  - `special_adjoint_functor_theorem`     → `saft_representability` — **PROVED**
  - `more_general_adjoint_functor_theorem` (⇐) → `mgaft_representability` — **PROVED**
- **Two STATEMENTS FIXED for faithfulness** (integrity, not cosmetic):
  - `more_general_adjoint_functor_theorem` gained the essential `IdempotentsSplit ℬ` hypothesis.
    Freyd §1.836 gives an explicit counterexample without it, so the previous statement was FALSE.
    Added `IdempotentSplits` / `IdempotentsSplit` (§1.281, restated locally).
  - `PreCocomplete` re-modeled on the new `HasPreColimit` (cofinal **cocones**) instead of the
    wrong-variance `HasPreLimit` (cofinal cones = lower bounds). This unblocked the genuinely-easy
    `(⇒)` half (`cocomplete_imp_preCocomplete`, axiom-free).

## Discharged GAFT + §1.837 this session (2026-06-16, second pass)

Built a constructive, axiom-clean representing-object engine and its colimit dual, discharging
**2 of the 4** previously-open sorries:

- **`gaft_representability`** — the General Adjoint Functor Theorem heart, fully constructive:
  - `InitialElement G A R` packages an initial object of `El(A,G-)` (element `θ : A⟶GR`, existence
    and uniqueness of factoring maps `x : R⟶B` with `θ≫Gx = g`); `InitialElement.represents` turns
    it into `RepresentedBy G A R` (the `φ_nat`/`φψ`/`ψφ` laws all follow from uniqueness).
  - `wideEqualizer heq hp e` — joint equalizer of a small family `K → (P⟶P)` against `id`, built as
    `eq(tupling e, tupling const-id) : ∏ₖ P` (so it reuses the existing equalizer + product API,
    avoiding any bespoke diagram shape that collides with `discCat82`).
  - the engine: solution set `(obj, maps)` from `PreAdjointFunctor`; `P := lim obj` (discrete
    product, so **continuity** supplies the element `η` with `η≫G(proj i)=maps i` and joint
    monicity of `{G proj}`); `(P,η)` weakly initial; `R :=` wide equalizer of all `A↓G`-endos of
    `(P,η)`; continuity on the equalizer gives the element `θ` (`θ≫Gr=η`); uniqueness of factoring
    maps via: equalize two candidates, factor `θ` through that equalizer (continuity), pull back
    along a weakly-initial map, then `wideEqualizer.spec` + monicity of `r` force a section.
  - `general_adjoint_functor_theorem` is now FULLY PROVED; `lean_verify` → `propext`,
    `Classical.choice` (no `sorryAx`).

- **`cocomplete_of_complete_precocomplete`** — §1.837 hard half, the colimit-DUAL of the GAFT
  engine, built entirely inside `ℬ` (NO functor category `ℬ^D` / diagonal `Δ` needed, contrary to
  the earlier blocker note): `P := ∏ⱼ Nⱼ` over the pre-colimit nadirs gives a weakly-initial cocone
  `(P,κ)`; the wide equalizer `R` of all cocone-endomorphisms is the colimit. Each `κ i` factors
  through `r` by joint monicity of the product (no continuity needed here); uniqueness dualizes the
  GAFT argument. `complete_cocomplete_iff_precocomplete` FULLY PROVED, axiom-clean.

## Discharged SAFT this session (2026-06-16, third pass)

- **`saft_representability`** (§1.838 SAFT) — FULLY PROVED, axiom-clean (`propext`,
  `Classical.choice`, `Quot.sound`; no `sorryAx`). Reduced to GAFT by building a
  `PreAdjointFunctor G` (`saft_preadjoint`) and feeding it to the proven `gaft_representability`
  engine. New supporting infra, all proved:
  - `cogenerating_embeds_in_product` — the §1.83(10) embedding (one direction): with all products a
    cogenerating set makes the evaluation map `B ⟶ ∏_{(i,h):B→Cᵢ} Cᵢ` monic. No choice, no
    completeness — collective faithfulness read off the `(i,h)`-projection.
  - `Cospan`/`gPullbackFactor` — walking-cospan diagram + "`G` continuous ⟹ `G` preserves the
    pullback of a mono": the pullback `S` of `L —w→ M ←eB— B` is a cospan limit, its `lft`-leg is
    monic (cone-lift uniqueness + `eB` monic), and continuity factors a commuting pair `(η,f)`
    through it.
  - `saft_preadjoint` — the SOLUTION SET. For each `A`: `PA := ∏_{j:Σi,(A→GCᵢ)} Cⱼ.₁` (built as a
    discrete limit so continuity gives the canonical `η:A→GPA` and joint-monicity of `{G proj}`);
    family indexed by `Σ (k:WPidx PA), (A⟶G(reprPA k).dom)` over the **well-powered** subobjects of
    `PA`. Cofinality of `f:A→GB`: embed `B` (`cogenerating_embeds_in_product`), comparison
    `w:PA→Q B`, the square `η≫Gw=f≫GeB` checked componentwise, pull back the mono `eB` along `w`
    (`gPullbackFactor`), transport the resulting subobject along the well-powered representative iso.
  - `special_adjoint_functor_theorem` — FULLY PROVED through the §1.817 bridge.

## Remaining sorries (0)

`mgaft_representability` (§1.835) is now **fully proved** — S1_82 has no sorries. It does NOT
reuse the GAFT engine (which needs full `Complete ℬ` for the product `∏ obj` and the genuine wide
equalizer). Construction under `PreComplete ℬ` + `IsUniformlyContinuous G` + `PreAdjointFunctor G`
+ `IdempotentsSplit ℬ`:

1. **Weakly-initial `(P, η)`.** Discrete pre-limit of the pre-adjoint solution objects `obj`;
   uniform continuity (`uniformly_continuous_preserves_prelimits`) makes its `G`-image cofinal,
   producing `η : A ⟶ G P` with `η ≫ G(proj i) = maps i`. Pre-adjoint cofinality ⟹ every
   `g : A ⟶ G B` factors as `η ≫ G w`.
2. **Canonical idempotent `e₀`.** New `wforkDiag` (wide-fork) shape: a cone over it is a map `q`
   with `q ≫ e = q` for every endo in a family. Apply it (via pre-limit + UC) to
   `M := {e : P⟶P // η ≫ G e = η}`, getting `q : Q ⟶ P` with `q ≫ e = q ∀ e∈M` and `η_Q` with
   `η_Q ≫ G q = η`. Weak-initial factoring `η ≫ G s = η_Q` gives `e₀ := s ≫ q`, with `e₀ ∈ M`
   and `e₀ ≫ e = e₀ ∀ e∈M`; in particular **`e₀ ≫ e₀ = e₀`**.
3. **Split ⟹ initial.** `IdempotentsSplit` splits `e₀ = ρ ≫ σ`, `σ ≫ ρ = id_R`. Then
   `(R, θ_R := η ≫ G ρ)` is an initial element of `El(A,G(-))` (`θ_R ≫ G σ = η`). Existence via
   weak initiality; uniqueness via `σ ≫ e = σ ∀ e∈M` together with a fresh wide-fork
   weak-equalizer of each candidate pair `(y₁, y₂)`.

`IdempotentsSplit` is load-bearing exactly at step 3 — without it the theorem is false (§1.836).
`lean_verify` axioms = `propext`, `Classical.choice` (no `sorryAx`); same profile as GAFT/SAFT.

## Key Theorems Proved

- **`complete_iff_eq_prod` (⇐)**: Given `HasEqualizers ℬ` and `HasProducts ℬ`, constructs
  `Complete ℬ` via the standard construction: lim D = eq(f, g) where
  f, g : ΠᵢDᵢ → Π_{x:i→j}Dⱼ encode the naturality condition (§1.825).

- **`limit_cone_unique`**: Any two limit cones over the same diagram are isomorphic;
  the mediating maps are mutual inverses proved by limit uniqueness (§1.822).

- **`uniformly_continuous_preserves_prelimits`**: A uniformly continuous functor sends
  any cone in 𝒜 to one factoring through a member of a cofinal family (§1.831).
